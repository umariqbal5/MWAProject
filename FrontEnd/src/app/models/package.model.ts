export interface PackageModel {
  _id: string;
  package: {
    name: string,
    package_type: string,
    description: string,
    image_url: string
  };
  destination: Array<string>;
  hotel: string;
  price: number;
}

const evilResponseProps = Object.keys(this.address);
const goodResponse = [];

for(const a of goodResponse) {
  goodResponse.push(goodResponse[a]);
}
